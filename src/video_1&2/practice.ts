// Welcome to TypeScript practice!
// You can run this file directly using ts-node:
// `ts-node practice.ts`
// Or compile it to JavaScript using tsc:
// `tsc practice.ts`

let isAwesome: boolean = true;
let greeting: string = "Hello, TypeScript!";
let num: number = 42;

function greet(name: string): string {
    return `${greeting} My name is ${name}.`;
}

console.log(greet("Developer"));
console.log(`TypeScript is awesome? ${isAwesome}`);
var x:number=20;
console.log(x);

var y:string="Hello";
console.log(y);